name: Mise à jour automatique des données
on:
  schedule:
    - cron: "0 5 * * *"
  workflow_dispatch: {}
permissions:
  contents: write
  issues: write
concurrency:
  group: update-data
  cancel-in-progress: false
jobs:
  update-data:
    runs-on: ubuntu-latest
    steps:
      - name: Récupérer le dépôt
        uses: actions/checkout@v7
      - name: Installer Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22"

      # Chaque source est indépendante : l'échec de l'une n'empêche pas la publication des autres.
      - name: Scrutins, dossiers et groupes (Assemblée nationale)
        id: scrutins
        run: node scripts/fetch-scrutins.js
      - name: Indicateurs (Insee, accès public sans clé)
        id: insee
        run: node scripts/fetch-insee.js
        continue-on-error: true
      - name: Sondages (liste Wikipédia, notices de la Commission des sondages)
        id: sondages
        run: node scripts/fetch-sondages.js
        continue-on-error: true

      - name: Contrôler la cohérence des données avant publication
        run: node scripts/check-data.js
      - name: Committer les changements s'il y en a
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/
          if git diff --cached --quiet; then
            echo "Aucun changement de données — rien à committer."
          else
            git commit -m "Mise à jour automatique des données ($(date -u +%Y-%m-%d))"
            git pull --rebase origin "${GITHUB_REF_NAME}"
            git push
          fi

      - name: Vérifier la fraîcheur des données
        id: fraicheur
        run: node scripts/check-fraicheur.js 2> fraicheur.txt
        continue-on-error: true

      # Alerte : un ticket « alerte-donnees » est ouvert (ou complété s'il existe déjà)
      - name: Signaler un problème par un ticket GitHub
        if: always() && (failure() || steps.insee.outcome == 'failure' || steps.sondages.outcome == 'failure' || steps.fraicheur.outcome == 'failure')
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          {
            echo "La mise à jour automatique du $(date -u +%Y-%m-%d) signale un problème."
            echo
            [ "${{ steps.scrutins.outcome }}" = "failure" ] && echo "- **Scrutins** : échec du script (les votes ne sont plus mis à jour)."
            [ "${{ steps.insee.outcome }}" = "failure" ] && echo "- **Insee** : au moins un indicateur n'a pas pu être mis à jour (valeur précédente conservée)."
            [ "${{ steps.sondages.outcome }}" = "failure" ] && echo "- **Sondages** : lecture de la liste Wikipédia impossible ou enquêtes écartées par les contrôles."
            [ -s fraicheur.txt ] && cat fraicheur.txt
            echo
            echo "Journal complet : $RUN_URL"
          } > alerte.md
          # Empreinte des problèmes signalés : on ne recommente pas chaque jour la même alerte
          empreinte=$(grep '^- ' alerte.md | sha256sum | cut -c1-16)
          echo "<!-- empreinte: $empreinte -->" >> alerte.md
          gh label create alerte-donnees --color D93F0B --description "Problème détecté par la mise à jour automatique" 2>/dev/null || true
          numero=$(gh issue list --label alerte-donnees --state open --json number --jq '.[0].number')
          if [ -n "$numero" ]; then
            dernier=$(gh issue view "$numero" --json body,comments --jq '(.comments | last | .body) // .body')
            if printf '%s' "$dernier" | grep -q "empreinte: $empreinte"; then
              echo "Alerte identique déjà signalée dans le ticket #$numero : rien à ajouter."
            else
              gh issue comment "$numero" --body-file alerte.md
            fi
          else
            gh issue create --title "Alerte : mise à jour automatique des données" --label alerte-donnees --body-file alerte.md
          fi
